import "outstream";
import "filestream";

struct Bob {
    string name;
    int age;
};

fin main() -> int {

    if(5 > 10) {
        return 5;
    }

    Bob bob = new Bob{name: "Bob", age: 30};

    print(bob);

    return 0;
}