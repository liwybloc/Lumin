import "outstream";

fin test(string ...args) {
    int length = args.length;
    for(int i=0;i<length;i++) {
        print(args[i]);
        if(i < length-1) {
            print(" ");
        }
    }
}

struct Bob {
    int age;
    string name;
};

fin main() -> int {

    Bob bob = { age: 30, "Bob 1" };
    bob.age = 50;
    println(bob);

    int[] x = [1,2,3,4,5];
    test("hi\n");
    return 0;
}